{
  "name": "vite-plugin-tsc-build",
  "version": "0.1.2",
  "description": "Vite plugin for creating your production build using tsc instead of esbuild",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^16.0.0",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "vite": "^4.0.0"
  }
}
